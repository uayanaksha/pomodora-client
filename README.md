# Pomodora timer client

## Getting started 

  - clone the repo 
  - `bun install` will fetch required dependencies
  - run `bun dev` to run the development environment on http://localhost:3000 
  - run `bun dev --host` to run the development environment over local network

## TODO 
  
  - UI/UX needs heavy improvements 
  - A model popup for notification of interval switch. (25 mins WORK; 5 mins BUFFER)
  - Responsive design

## Deployment
  
  - deploy url: [pomodora-ayanaksha.vercel.app](https://pomodora-ayanaksha.vercel.app)
